<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backform.mapper.LikeMapper">
<select id="existsLike" resultType="int">select count(*) from article_likes where article_id=#{articleId} and user_id=#{userId}</select>
<insert id="insertLike">insert into article_likes(article_id,user_id) values(#{articleId},#{userId})</insert>
<delete id="deleteLike">delete from article_likes where article_id=#{articleId} and user_id=#{userId}</delete>
</mapper>
