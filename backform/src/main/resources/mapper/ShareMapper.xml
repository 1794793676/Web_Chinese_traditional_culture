<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backform.mapper.ShareMapper">
<insert id="insertShare">insert into article_shares(article_id,user_id,share_channel) values(#{articleId},#{userId},#{channel})</insert>
</mapper>
